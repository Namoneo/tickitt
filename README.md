# Tickitt

AI-driven ticketing workspace.

## Phase 0

Electron + Angular 19 skeleton with SQLite, tRPC IPC, and keychain secrets.

## Scripts

- `pnpm dev` — start dev
- `pnpm build` — production build
- `pnpm package:dir` — package macOS app
